rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Development: Allow all reads and writes (for emulator only)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
